{
  "name": "AgentLens Extension Dev",
  "image": "mcr.microsoft.com/devcontainers/universal:2",
  "postCreateCommand": "python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt && cd agentlens-vscode && npm install && npm run compile",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "dbaeumer.vscode-eslint"
      ],
      "settings": {
        "agentlens.cacheRoot": "${containerWorkspaceFolder}/agentlens-vscode/cache",
        "agentlens.projectRoot": "${containerWorkspaceFolder}"
      }
    }
  },
  "forwardPorts": [8765]
}
